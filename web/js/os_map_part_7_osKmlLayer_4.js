/**
 * Model de kmls
 * Author RNE
 * CanalTP 2013
 * @type @exp;Backbone@pro;Model@call;extend
 */
var ctpKmlLayer = CanalTP.lib.map.models.marker.extend({
    /**
     * Fonction permettant l'initialisation
     */
    initialize: function()
    {
        return false;
    }
});